{
  "name": "vibe-slide-root",
  "version": "1.0.0",
  "description": "Root package for Vibe Slide",
  "scripts": {
    "dev": "concurrently \"docker compose --env-file .env.local up\" \"cd backend && NODE_ENV=local bun run dev\" \"cd frontend && bun run dev\"",
    "down": "docker compose down"
  },
  "devDependencies": {
    "concurrently": "^9.2.1"
  }
}
